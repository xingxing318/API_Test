# 本地代理（用于绕过浏览器 CORS）

## 启动
在项目根目录运行：

```bash
node proxy/server.mjs
```

默认监听：`http://127.0.0.1:8787`

可用环境变量：
- `PORT`：端口（默认 8787）
- `HOST`：监听地址（默认 127.0.0.1）

## 前端使用
在页面里将“连接方式”切到“本地代理”，并把“代理地址”设置为对应地址即可。

## 安全说明
该代理仅做本机转发，不做鉴权；请勿暴露到公网。

